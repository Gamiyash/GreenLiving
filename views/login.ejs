<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    /* Add your existing styles here */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    @keyframes slideIn {
      from {
        transform: translateY(-20px);
        opacity: 0;
      }

      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.05);
      }

      100% {
        transform: scale(1);
      }
    }

    body {
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      animation: fadeIn 1s ease-out;
    }

    .login-container {
      display: flex;
      width: 80%;
      max-width: 800px;
      background-color: #fff;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      animation: slideIn 0.8s ease-out;
      background: rgba(255, 255, 255, 0.34);
      border-radius: 16px;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
      backdrop-filter: blur(8.1px);
      -webkit-backdrop-filter: blur(8.1px);
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .login-form-wrapper {
      padding: 40px;
      flex: 1;
      text-align: center;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 30px;
      color: #333;
      animation: slideIn 0.8s ease-out 0.2s both;
    }

    .form-group {
      margin-bottom: 20px;
      animation: slideIn 0.8s ease-out 0.4s both;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: bold;
      color: #555;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 12px;
      border: 2px solid #ddd;
      border-radius: 8px;
      box-sizing: border-box;
      transition: border-color 0.3s ease;
    }

    input[type="email"]:focus,
    input[type="password"]:focus {
      border-color: #4CAF50;
      outline: none;
    }

    button[type="submit"] {
      background-color: #4CAF50;
      color: #fff;
      padding: 15px 20px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      width: 100%;
      margin-top: 20px;
      font-size: 16px;
      transition: all 0.3s ease;
      animation: slideIn 0.8s ease-out 0.6s both;
    }

    button[type="submit"]:hover {
      background-color: #45a049;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .new-user {
      margin-top: 30px;
      animation: slideIn 0.8s ease-out 0.8s both;
    }

    .new-user span {
      margin-right: 10px;
      color: #666;
    }

    .new-user a {
      color: #054507;
      text-decoration: none;
      font-weight: bold;
      transition: color 0.3s ease;
    }

    .new-user a:hover {
      color: #022103;
      text-decoration: underline;
    }

    .tree-animation {
      flex: 1;
      background: url('https://www.svgrepo.com/show/530293/tree-2.svg') center/cover no-repeat;
      position: relative;
      overflow: hidden;
    }

    .tree-animation::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(76, 175, 80, 0.1);
      animation: pulse 3s infinite ease-in-out;
    }

    .green-leaf-decoration {
      position: absolute;
      top: 20px;
      left: 20px;
      width: 50px;
      height: 50px;
      background: url('https://www.svgrepo.com/show/530305/leaves-5.svg') center/contain no-repeat;
      animation: fadeIn 1s ease-out 1s both, pulse 3s infinite ease-in-out;
      z-index: -1;
    }

    .green-leaf-decoration2 {
      position: absolute;
      top: 20px;
      left: 20px;
      width: 50px;
      height: 50px;
      background: url('https://www.svgrepo.com/show/414844/leaf-autumn-fall.svg') center/contain no-repeat;
      animation: fadeIn 1s ease-out 1s both, pulse 3s infinite ease-in-out;
      z-index: -1;
    }

    .google {
      text-align: center;
    }

    @keyframes fall {
      0% {
        transform: translateY(-100vh) rotate(0deg);
      }

      100% {
        transform: translateY(100vh) rotate(360deg);
      }
    }

    .leaf {
      position: absolute;
      background-image: url('https://images.unsplash.com/photo-1649256381294-2462d2614ef8?q=80&w=2073&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
      height: 100vh;
      width: 100vw;
      z-index: -1;
      background-size: cover;
    }

    @media (max-width: 768px) {
      .login-container {
        flex-direction: column;
      }

      .tree-animation {
        height: 200px;
      }
    }

    .error-message {
      color: #ff3333;
      margin-top: 15px;
    }

    /* .google-signin-btn {
      border: 2px black;
      padding: 5px;
      background-color: rgb(213, 74, 74);
      text-decoration: none;
      color: black;
      border-radius: 5px;
    } */

    .google-signin-btn > img{
      width: 60px;
      /* height: 40px; */
      margin-right: 5px;
      border-radius: 20px;
    }
  </style>
</head>

<body>
  <div class="login-container">
    <div class="login-form-wrapper">
      <form action="/auth/login" method="POST">
        <h1>Welcome Back</h1>
        <% if (typeof error !=='undefined' ) { %>
          <p class="error-message">
            <%= error %>
          </p>
          <% } %>
            <div class="form-group">
              <label for="email">Email:</label>
              <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
              <label for="password">Password:</label>
              <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Login</button>
      </form>
      <div class="google">
        <p>Or sign in with Google:</p>
        <a href="/auth/google" class="google-signin-btn">
          <img src="https://firebasestorage.googleapis.com/v0/b/greenlander-c8556.appspot.com/o/google.png?alt=media&token=ac884d77-24b0-411b-80e9-02feeb00769c" alt="">
          <!-- Sign in with Google -->
        </a>
      </div>
      <div class="new-user">
        <span>New User?</span>
        <a href="/auth/signup">Register</a>
      </div>
    </div>
    <div class="tree-animation"></div>
  </div>
  <div class="leaf"></div>
  <div class="green-leaf-decoration"></div>
  <div class="green-leaf-decoration2"></div>
</body>

</html>